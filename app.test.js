describe('dan method test', () => {

  it('creates a dan method', () => {
    const res = 'danMethod';
    expect(res).toEqual('danMethod');
  });
});

//had to try a new testing method to see if it'll test

